---
import { Image } from "astro:assets";
import animoji from "../animoji.png";
---

<div class="relative h-screen">
  <slot />
  <div class="absolute inset-0 h-screen filter blur-3xl">
    <div
      class="absolute w-8 md:w-16 h-40 md:h-80 bg-green-500 transform left-1/2 top-1/4 rotate-[10deg]"
    >
    </div>
    <div
      class="absolute w-6 md:w-12 h-48 md:h-96 bg-green-500 transform left-1/4 top-1/2 rotate-[-15deg]"
    >
    </div>
  </div>
  <div class="flex z-10 h-screen flex-col md:flex-row">
    <!-- Textbereich -->
    <div class="flex-1 flex items-center justify-center p-4">
      <div class="text-center md:text-left">
        <h1 class="text-white text-2xl md:text-xl">Hi there 👋</h1>
        <p class="text-white text-3xl md:text-2xl">
          I’m Luis, a Fullstack Developer
        </p>
      </div>
    </div>

    <!-- Bildbereich -->
    <div class="flex-1 flex items-center justify-center p-4">
      <div
        class="w-48 h-48 md:w-96 md:h-96 bg-green-500 rounded-full overflow-hidden flex items-center justify-center"
      >
        <Image src={animoji} alt="animoji" class="object-cover w-full h-full"  loading="eager" />
      </div>
    </div>
  </div>
</div>
